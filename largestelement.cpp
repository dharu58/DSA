#include <bits/stdc++.h>

int largestelement (vector<int> &arr, int n){
    int largest = a[0];
    for (int i = 0; i < n; i++){
        if (arr[i] > largest){
            largest = arr[i];

        }
        }
        return largest;
}